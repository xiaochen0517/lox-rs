fun parent() {
    var x = "outside";

    fun nested() {
        x = x + " inside";
        print x + "\n";
    }

    return nested;
}

var closure = parent();
closure(); // Should print "outside inside"
closure(); // Should print "outside inside inside"
